<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mã Xác Minh – Elysia Style (Honkai)</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #9b59b6 0%, #e75480 50%, #ff6b81 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
            color: #f0f0f0;
        }

        /* background particles/glow */
        body::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background-image:
                radial-gradient(circle, rgba(255,255,255,0.08) 1px, transparent 1px),
                radial-gradient(circle, rgba(255,150,200,0.06) 1px, transparent 1px);
            background-size: 40px 40px, 70px 70px;
            background-position: 0 0, 35px 35px;
            animation: drift 25s linear infinite;
            opacity: 0.7;
        }
        @keyframes drift {
            0% { transform: translate(0,0); }
            100% { transform: translate(60px,60px); }
        }

        .container {
            background: rgba(20, 20, 30, 0.95);
            border-radius: 30px;
            box-shadow: 0 30px 80px rgba(0,0,0,0.6);
            max-width: 600px;
            width: 100%;
            padding: 50px 40px;
            position: relative;
            z-index: 1;
            border: 2px solid rgba(255,255,255,0.2);
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .logo {
    width: 120px;
    height: 120px;
    margin: 0 auto 20px;
    position: relative;
    border-radius: 50%;
    overflow: hidden; /* ✅ giúp ảnh bo tròn */
    box-shadow: 0 10px 30px rgba(231, 84, 128, 0.5);
    animation: float 3s ease-in-out infinite;
}

.logo img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* ✅ giữ tỉ lệ đẹp */
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
}

.logo::after {
    content: '';
    position: absolute;
    top: -10px;
    left: -10px;
    width: 140px;
    height: 140px;
    border: 3px solid rgba(231, 84, 128, 0.4);
    border-radius: 50%;
    animation: ripple 2s ease-out infinite;
}

        @keyframes float {
            0%,100% { transform: translateY(0px); }
            50% { transform: translateY(-12px); }
        }
        @keyframes pulse {
            0%,100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
        @keyframes ripple {
            0% { transform: scale(1); opacity: 1; }
            100% { transform: scale(1.6); opacity: 0; }
        }

        h1 {
            color: #e75480;
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 700;
        }
        .subtitle {
            color: #ccc;
            font-size: 16px;
            line-height: 1.6;
        }

        .divider {
            height: 2px;
            background: linear-gradient(90deg, transparent, #e75480, #9b59b6, transparent);
            margin: 30px 0;
        }

        .content {
            text-align: center;
        }

        .message {
            color: #ddd;
            font-size: 16px;
            margin-bottom: 30px;
            line-height: 1.8;
        }

        .code-container {
            background: linear-gradient(135deg, #9b59b6 0%, #e75480 100%);
            border-radius: 20px;
            padding: 30px;
            margin: 30px 0;
            position: relative;
            overflow: hidden;
        }
        .code-container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255,255,255,0.12), transparent);
            animation: shine 3s infinite;
        }
        @keyframes shine {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .code-label {
            color: rgba(255,255,255,0.9);
            font-size: 14px;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .verification-code {
            background: white;
            color: #9b59b6;
            font-size: 42px;
            font-weight: 900;
            letter-spacing: 12px;
            padding: 20px;
            border-radius: 15px;
            margin: 0 auto;
            display: inline-block;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            font-family: 'Courier New', monospace;
            position: relative;
            z-index: 1;
            user-select: all;
        }
        .timer {
            color: rgba(255,255,255,0.9);
            font-size: 14px;
            margin-top: 15px;
        }

        .info-box {
            background: linear-gradient(135deg, #3c2a4c 0%, #4f3560 100%);
            border-left: 4px solid #e75480;
            padding: 20px;
            border-radius: 10px;
            margin: 30px 0;
            text-align: left;
        }
        .info-box p {
            color: #bbb;
            font-size: 14px;
            line-height: 1.8;
            margin-bottom: 10px;
        }
        .info-box p:last-child {
            margin-bottom: 0;
        }
        .info-box strong {
            color: #e75480;
        }

        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid rgba(255,255,255,0.2);
        }
        .footer p {
            color: #aaa;
            font-size: 13px;
            line-height: 1.6;
        }
        .signature {
            color: #e75480;
            font-style: italic;
            margin-top: 15px;
            font-size: 16px;
        }

        /* optional sparkles */
        .sparkle {
            position: absolute;
            width: 6px;
            height: 6px;
            background: #e75480;
            border-radius: 50%;
            animation: sparkle 2.5s ease-in-out infinite;
        }
        .sparkle:nth-child(1) { top: 15%; left: 12%; animation-delay: 0s; }
        .sparkle:nth-child(2) { top: 65%; left: 88%; animation-delay: 0.6s; }
        .sparkle:nth-child(3) { top: 78%; left: 22%; animation-delay: 1.2s; }
        .sparkle:nth-child(4) { top: 42%; right: 18%; animation-delay: 1.8s; }
        @keyframes sparkle {
            0%,100% { opacity: 0; transform: scale(0); }
            50% { opacity: 1; transform: scale(1); }
        }

        @media (max-width: 600px) {
            .container {
                padding: 40px 25px;
            }
            h1 {
                font-size: 26px;
            }
            .verification-code {
                font-size: 32px;
                letter-spacing: 8px;
                padding: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="sparkle"></div>
    <div class="sparkle"></div>
    <div class="sparkle"></div>
    <div class="sparkle"></div>

    <div class="container">
        <div class="header">
            <div class="logo">
    <img src="elysia_logo.png" alt="Elysia Logo">
</div>
            <h1>Mã Xác Minh</h1>
            <p class="subtitle">Thay đổi mật khẩu♡</p>
        </div>

        <div class="divider"></div>

        <div class="content">
            <p class="message">
                Đây là mã xác thực của bạn
            </p>

            <div class="code-container">
                <div class="code-label">Mã xác thực</div>
                <div class="verification-code">{code}</div>
                <div class="timer">⏰ Mã có hiệu lực trong 3:01
            </div>

            <div class="info-box">
                <p><strong>📌 Lưu ý quan trọng:</strong></p>
                <p>• Mã xác minh này chỉ sử dụng được một lần duy nhất</p>
                <p>• Không chia sẻ mã này với bất kỳ ai</p>
                <p>• Nếu bạn không thực hiện yêu cầu này, vui lòng bỏ qua email</p>
                <p>• Mã sẽ hết hạn sau 3 phút kể từ khi nhận được</p>
            </div>
        </div>

        <div class="footer">
            <p>Nếu bạn gặp bất kỳ vấn đề gì, đừng ngại liên hệ với chúng tôi!</p>
            <p class="signature">~ With love, Elysia ♡</p>
        </div>
    </div>

    <script>
        // Đếm ngược thời gian
        let timeLeft = 180; // 10 phút
        const timerElement = document.querySelector('.timer');

        function updateTimer() {
            const minutes = Math.floor(timeLeft / 60);
            const seconds = timeLeft % 60;
            timerElement.textContent = `⏰ Mã có hiệu lực trong ${minutes}:${seconds.toString().padStart(2,'0')}`;

            if (timeLeft > 0) {
                timeLeft--;
            } else {
                timerElement.textContent = '⚠️ Mã đã hết hạn';
                timerElement.style.color = '#ff4d4d';
            }
        }

        setInterval(updateTimer, 1000);
    </script>
</body>
</html>
